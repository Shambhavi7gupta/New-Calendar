import { createContext } from "react";

// Create a context for sharing state and functions across the component tree
const WebContext = createContext();

// Export the created context to be used in other components
export default WebContext;
